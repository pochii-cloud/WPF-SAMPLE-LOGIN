<Window x:Class="login_sample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:login_sample"
        mc:Ignorable="d"
        Title="MainWindow"
         Height="450"
        Width="800"
        >
    <StackPanel>
        <Grid Width="400" HorizontalAlignment="Center" Margin="0 50">
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <TextBox Grid.Row="0" Name="usernameTextBox" Text="username" VerticalAlignment="Bottom" Padding="10" Margin="0 10"/>
            <TextBox Grid.Row="1" Name="emailTextBox" Text="email" Padding="10"/>
            <Button Grid.Row="2" Margin="0 10" Click="Button_Click" Content="Submit" Height="30"></Button>
        </Grid>
    </StackPanel>
</Window>
